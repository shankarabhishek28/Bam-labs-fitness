/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['lh3.googleusercontent.com','bam-labs-media.s3.ap-south-1.amazonaws.com'], // Add the domain hosting the images
    },
  };
  
  export default nextConfig;
  